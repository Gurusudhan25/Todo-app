export interface ITodo {
  task: string;
  description: string;
  state: boolean;
  // isLoading:boolean
}

export interface ITodoID {
  id: string;
  task: string;
  description: string;
  state: boolean;
  isLoading: boolean;
}
