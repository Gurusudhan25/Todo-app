export interface ITodo {
  task: string;
  description: string;
  state: boolean;
  tags: string[];
}

export interface ITodoID {
  id: string;
  task: string;
  description: string;
  state: boolean;
  tags: string[];
}
